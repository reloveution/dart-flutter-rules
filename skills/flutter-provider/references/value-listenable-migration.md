---
title: ValueListenableProvider Migration
description: Migrate to ValueListenableBuilder + Provider.value
---

# Migration

- Migrate ValueListenableProvider usage.
- Use `ValueListenableBuilder` combined with `Provider.value` instead.
- When only a listenable value must be exposed.
- ValueListenableProvider is deprecated/superseded by this pattern.
